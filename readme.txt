Dependencies: npm,bower,gulp
To install stack: "npm install && bower install"
First time: run "gulp" (creates 'dev' folder)
After that you can just run "gulp watch" (watches: 'assets' and builds to 'dev')
To build final/minified version: "gulp build" (folder: build)
